name: Analytics Infrastructure

on:
  push:
    branches:
      - 'codex/**'
    paths:
      - '.github/workflows/analytics-infra.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "analytics dispatch probe"
