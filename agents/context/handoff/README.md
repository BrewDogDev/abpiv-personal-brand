# Context Handoffs

This directory is for non-Project continuity records.

- Store dated handoffs as `YYYY-MM-DD-<goal-slug>-handoff.md`.
- Create or update `latest.md` only when a non-Project handoff exists.
- Keep Project continuity inside that Project's `handoff/` directory and update only the Project registry through its coordinator.
- Preserve prior dated handoffs and keep all handoffs in Layer 4.

No non-Project handoff is currently active.
