# Agent Organization V2 Migration Plan

## Planning Basis

- Goal: migrate live agent infrastructure to the 0.1.36 `agents/*` owner layout and procedures while preserving discovery, history, metadata, and safety boundaries
- Base state: clean topic branch `codex/migrate-agent-organization-v2` from `origin/preview` commit `088ac31aeea018131a7bf4d11fff8943266cfba1`; `origin/main` is `54c689259ff46a3901a6e01fa63cbf77ee214387`
- Approval basis: the original request explicitly authorizes the in-scope migration and necessary tracked moves
- Execution: one bounded implementer and one independent reviewer per Task
- Control state: this file is the authoritative Task ledger
- Replanning: material changes return through DDD and grill-me

## Shared Constraints

- Preserve root `AGENTS.md` discovery, `agents/context/`, implementation-domain behavior, same-origin analytics guidance, and every secret boundary.
- Move live owner surfaces with Git history; do not recreate human-authored content from memory.
- Treat `agents/context/projects/archive/` and `agents/context/runs/legacy/` as immutable historical evidence even when they contain former live paths.
- Never read, copy, log, link, or commit `.codex-local/` values or machine-local installed-cache paths.
- Adopt `BrewDogDev/abpiv-agents` 0.1.36's 23 non-generated `agent-organization` source files and exclude generated bytecode.
- Retain and validate the 14 existing `agents/openai.yaml` skill metadata files that are absent from the 0.1.36 subset.
- Keep canonical owner bodies portable and harness mappings thin.
- Update active paths, relative links, commands, repository maps, adapters, and validation contracts atomically within their owners.
- Use exact staging, inspect staged diffs, scan for secrets and machine-local paths, and never force push.
- Do not push, create or merge a pull request, deploy, publish, access credentials, broaden permissions, or mutate runtime or external state.
- Do not change application code, implementation behavior, infrastructure definitions, or repository promotion rules.

## Task Ledger

| Task | Outcome | Dependencies | Status | Implementer | Review |
| --- | --- | --- | --- | --- | --- |
| 01 | Move the complete vendored skill family to `agents/skills/` and migrate its supplied bodies and scripts to 0.1.36 while retaining repository metadata. | None | ready | unassigned | pending |
| 02 | Move the credential-free access registry to `agents/access/` with its internal contracts and live references intact. | None | planned | unassigned | pending |
| 03 | Move the MCP server registry to `agents/mcp-servers/` and preserve its access-profile and safety relationships. | 02 | planned | unassigned | pending |
| 04 | Move the empty executable-tool registry to `agents/tools/` without inventing a tool contract. | None | planned | unassigned | pending |
| 05 | Move the empty template registry to `agents/templates/` without inventing reusable templates. | None | planned | unassigned | pending |
| 06 | Move the harness adapter registry to `agents/adapters/` and update its mappings to the migrated canonical owners. | 01, 02, 03, 04, 05 | planned | unassigned | pending |
| 07 | Integrate the new owner layout into canonical context, routing, repository maps, verification, and repository entrypoints. | 01, 02, 03, 04, 05, 06 | planned | unassigned | pending |
| 08 | Perform whole-Project review, integrated verification, preservation checks, and reusable-learning assessment. | 07 | planned | unassigned | pending |

## Task Outcomes And Interfaces

### Task 01: Migrate the vendored agent-organization skill family

- Owns: existing `skills/`, target `agents/skills/`, all nested reusable skill bodies, references, scripts, retained interface metadata, and provenance documentation
- Consumes: the complete current 37-file family, package metadata for `BrewDogDev/abpiv-agents` 0.1.36, its 23 non-generated source files, and the repository's existing portability and discovery contracts
- Produces: a 37-file family under `agents/skills/` containing the 0.1.36 supplied subset plus 14 retained metadata files, with provenance and deviations stated without a machine-local path
- Required capabilities: `agent-skill-organization`, `testing-agent-skills`, and return through `agent-organization`
- Implementer selection: `deep` capability and `high` reasoning because the Task combines a structural move, semantic validator migration, provenance reconciliation, and compatibility preservation
- Reviewer selection: `deep` capability and `high` reasoning with `rigorous` depth because migration and compatibility defects could invalidate Project control state or recursive discovery
- Isolation: owns only the skills surface and its Task return artifact; it does not edit context routes, adapters, other owner surfaces, or Project control state
- Acceptance: normalized source inventory and content checks pass; generated bytecode is absent; retained metadata count and skill-name matching pass; recursive discovery is demonstrated; bundled validator tests pass from the target path
- Approval or stop gate: stop if the 0.1.36 subset cannot be reconciled with retained metadata or the active Project cannot satisfy the migrated validator

### Task 02: Relocate external-service access contracts

- Owns: existing `access/`, target `agents/access/`, and access-owned live internal path references
- Consumes: the current credential-free service contexts, profiles, interfaces, local-binding guidance, and secret boundary
- Produces: the same access contracts under `agents/access/` with resolving internal links and no credential reads or values
- Required capabilities: `agent-access-organization` and return through `agent-organization`
- Implementer selection: `balanced` capability and `medium` reasoning because the move is bounded but crosses profiles, interfaces, relative links, and safety text
- Reviewer selection: `balanced` capability and `medium` reasoning with `quick` depth because direct inventory, link, and secret-boundary checks strongly verify a documentation-only move
- Isolation: owns only the access surface and its Task return artifact
- Acceptance: all 14 files are preserved under the target path; access-owned links resolve; stable handles, gates, and secret language are unchanged; no credential value or cache path appears
- Approval or stop gate: stop if a move would lose history, expose a secret, or require changing an external target or permission

### Task 03: Relocate MCP server contracts

- Owns: existing `mcp-servers/`, target `agents/mcp-servers/`, and MCP-owned live references
- Consumes: Task 02's migrated access profile interface and the current n8n instance MCP contract
- Produces: the MCP registry under `agents/mcp-servers/` with resolving access-profile links and unchanged runtime, tool-discovery, and safety boundaries
- Required capabilities: `agent-mcp-organization` and return through `agent-organization`
- Implementer selection: `balanced` capability and `medium` reasoning because the move is small but the access/runtime boundary must remain precise
- Reviewer selection: `balanced` capability and `medium` reasoning with `quick` depth because the change is documentation-only and directly verifiable
- Isolation: owns only the MCP surface and its Task return artifact after Task 02 completes
- Acceptance: both files are preserved at the target path; all MCP-owned links resolve; no server executable, runtime mutation, credential value, or tool-inventory invention is introduced
- Approval or stop gate: stop if the target access profile does not resolve or runtime or secret scope would change

### Task 04: Relocate the executable-tool registry

- Owns: existing `tools/`, target `agents/tools/`, and the tool registry placement contract
- Consumes: the current empty registry and the 0.1.36 canonical owner layout
- Produces: the unchanged empty registry under `agents/tools/`
- Required capabilities: `agent-tool-organization` and return through `agent-organization`
- Implementer selection: `fast-repeatable` capability and `low` reasoning because one low-consequence file moves with deterministic content and path checks
- Reviewer selection: `fast-repeatable` capability and `low` reasoning with `quick` depth because exact blob and placement verification is mechanical
- Isolation: owns only the tool registry and its Task return artifact
- Acceptance: the registry content is preserved, the old path is absent, the new path exists, and no tool contract is invented
- Approval or stop gate: stop if any live executable tool appears during execution or another change overlaps the registry

### Task 05: Relocate the reusable-template registry

- Owns: existing `templates/`, target `agents/templates/`, and the template registry placement contract
- Consumes: the current empty registry and the 0.1.36 canonical owner layout
- Produces: the unchanged empty registry under `agents/templates/`
- Required capabilities: `agent-organization`
- Implementer selection: `fast-repeatable` capability and `low` reasoning because one low-consequence file moves with deterministic content and path checks
- Reviewer selection: `fast-repeatable` capability and `low` reasoning with `quick` depth because exact blob and placement verification is mechanical
- Isolation: owns only the template registry and its Task return artifact
- Acceptance: the registry content is preserved, the old path is absent, the new path exists, and no template is invented
- Approval or stop gate: stop if a reusable template appears during execution or another change overlaps the registry

### Task 06: Relocate and remap harness adapters

- Owns: existing `adapters/`, target `agents/adapters/`, and adapter-owned Codex and historical Kilo mappings
- Consumes: Tasks 01 through 05's canonical target paths and the existing thin adapter contracts
- Produces: adapters under `agents/adapters/` whose live mappings point to `agents/context/`, `agents/skills/`, `agents/access/`, `agents/mcp-servers/`, `agents/tools/`, and `agents/templates/` without duplicating bodies
- Required capabilities: `agent-adapter-organization` and return through `agent-organization`
- Implementer selection: `balanced` capability and `medium` reasoning because the Task updates multiple harness mappings after a structural migration
- Reviewer selection: `deep` capability and `high` reasoning with `rigorous` depth because stale discovery paths could silently break supported harness behavior
- Isolation: owns only the adapter surface and its Task return artifact after canonical owner moves complete
- Acceptance: all three adapter files are preserved at the target path; active Codex paths resolve; Kilo remains historical; adapters stay mapping-only; no machine-local configuration is introduced
- Approval or stop gate: stop if recursive discovery cannot be demonstrated or a harness mapping would require duplicate canonical instructions

### Task 07: Integrate canonical context and repository governance

- Owns: live non-archived files under `agents/context/`, root `AGENTS.md`, root `README.md` when referenced, and other active repository documentation that routes to moved agent owners
- Consumes: Tasks 01 through 06's final paths, the current repository map, verification contract, Git policy, glossary, and immutable-history boundaries
- Produces: coherent live context, routing, repository-map, verification, and entrypoint references for the `agents/*` owner layout
- Required capabilities: `agent-context-organization` and return through `agent-organization`
- Implementer selection: `deep` capability and `high` reasoning because the Task integrates multiple canonical owners and must distinguish live routes from immutable history and unrelated implementation strings
- Reviewer selection: `deep` capability and `high` reasoning with `rigorous` depth because cross-domain routing and validation drift could make future agent runs unsafe or undiscoverable
- Isolation: owns context and entrypoint integration only; it does not rewrite owner bodies, immutable archives, legacy history, implementation code, or Project coordinator control state
- Acceptance: every active agent-owner path and changed local Markdown link resolves; verification commands use target paths; no absent Workflow, tool, access profile, MCP server, adapter, or template is invented; archives and legacy history remain byte-for-byte unchanged
- Approval or stop gate: stop if live and historical references cannot be distinguished or a repository workflow contradicts current governance

### Task 08: Integrated review and verification

- Owns: Task 08 report and review evidence only
- Consumes: the complete migration diff, all prior Task reports and reviews, Project completion criteria, source-package inventory, and baseline archive hashes
- Produces: independent whole-Project verdict, validator and scenario evidence, recursive-discovery and metadata evidence, active-link and stale-path evidence, preservation evidence, secret and cache-path scan results, and exact Git scope
- Required capabilities: `agent-organization`, `testing-agent-skills`, and `verification-before-completion`
- Implementer selection: `deep` capability and `high` reasoning because the Task must integrate structural, semantic, safety, provenance, history, and Git evidence
- Reviewer selection: `deep` capability and `high` reasoning with `rigorous` depth because this is a migration-wide compatibility and preservation gate
- Isolation: read-only outside its own Task return artifact; findings return to the coordinator for replanning or a bounded fix Task
- Acceptance: every Project completion criterion has direct fresh evidence; all validators and deterministic scenarios pass; retained metadata and recursive discovery pass; active stale-path scans are clean; archives and legacy runs match baseline; the diff contains only authorized agent-infrastructure changes
- Approval or stop gate: any blocking finding, failed required check, changed immutable history, secret-shaped value, machine-local cache path, or unrelated diff returns the Project to planning

## Integration And Reuse Obligations

- Canonical context, glossary, routing, Workflow, and repository docs: Task 07
- Validated reusable procedures or operational knowledge: Tasks 01 and 08 assess; Task 07 integrates only reviewed durable repository guidance
- Project-wide review and integrated verification: Task 08
- Project-local continuity and immutable archival: coordinator after all Task reviews and integrated verification pass

## Checkpoints And Replanning

- Tasks 01, 02, 04, and 05 are structurally independent and parallel-eligible only in isolated worktrees; default execution is sequential to keep one shared branch reviewable.
- Tasks 03, 06, 07, and 08 are sequential according to their dependencies.
- Record completed Task commit ranges, review verdicts, deviations, and unresolved Minor findings in the ledger or corresponding Task section.
- Add dated amendments to `PROJECT.md` and revise this graph before dispatch when live evidence changes scope, architecture, domain boundaries, acceptance, or dependencies.

## Project Closure

- All ledger Tasks are `complete`.
- All independent reviews and blocking fixes pass.
- Integrated verification proves the Project completion criteria.
- A Project-local handoff records branch, commits, checks, unpublished state, and a fresh-session continuation prompt if anything remains.
- The coordinator removes the active route and moves the intact Project to `archive/2026-07-27-agent-organization-v2-migration/`.
