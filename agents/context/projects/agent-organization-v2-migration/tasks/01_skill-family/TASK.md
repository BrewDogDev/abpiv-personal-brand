# Task 01: Migrate the vendored agent-organization skill family

## Status

Ready

## Parent Project And Live Basis

- Project: `agents/context/projects/agent-organization-v2-migration/PROJECT.md`
- Plan row: 01
- Planned from: `codex/migrate-agent-organization-v2` at coordinator commit `2cd4a03c197954648418c43857079876a1e0363c`
- Refreshed at: 2026-07-27 at coordinator commit `2cd4a03c197954648418c43857079876a1e0363c`
- Dependencies verified: None; the worktree was clean after the Project-planning commit, the current complete family contains 37 non-generated files, and direct Task preflight proved that the requested 0.1.36 source subset contains 24 non-generated files plus one excluded generated bytecode file.

## Role, Outcome, And Acceptance

- Role: bounded skill-family migration implementer
- Outcome: the complete repository-owned `agent-organization` family lives under `agents/skills/` and applies the supplied `BrewDogDev/abpiv-agents` 0.1.36 bodies and scripts while preserving repository-required Codex metadata and portable provenance.
- Acceptance criteria:
  - Move the complete tracked `skills/` tree to `agents/skills/` with Git-visible history and no content loss.
  - Overlay exactly the 24 non-generated relative files supplied by `BrewDogDev/abpiv-agents` 0.1.36's `skills/agent-organization/` subset, including the directly linked `agent-project-organization/references/capability-and-reasoning-classes.md`; never copy `__pycache__`, `.pyc`, or `.pyo` output.
  - Retain all 14 existing `agents/openai.yaml` interface metadata files beside their matching skills; each remains valid, matches the folder skill name, and invokes that skill.
  - Update `agents/skills/README.md` to identify `https://github.com/BrewDogDev/abpiv-agents` version 0.1.36 as the selected source, state the 24 supplied files plus 14 retained metadata files, document target-layout and retained-metadata deviations, and avoid any installed-cache path.
  - Require 14 recursively discovered, globally unique, folder-matched `SKILL.md` files and 14 matching metadata files.
  - Require the target Project-validator tests to finish with `OK`, and require the live Project and Workflow validators from `agents/skills/` to finish without errors or warnings.
  - Preserve the existing upstream license resource and useful human-authored repository portability guidance unless the 0.1.36 contract supersedes it.

## Relevant Context And Source Paths

- The user selected `BrewDogDev/abpiv-agents` package version 0.1.36 through the active `abpiv-agents:agent-organization` skill.
- The source package metadata declares repository `https://github.com/BrewDogDev/abpiv-agents`, version 0.1.36, and MIT licensing.
- Runtime source location must be resolved from the active installed package supplied at dispatch; it is an input only and must never be written into a tracked file, report, diff, or command transcript.
- The 0.1.36 family subset contains 24 non-generated files, including the new capability/reasoning reference, and intentionally omits the 14 Codex `agents/openai.yaml` files that this repository's current discovery contract requires.
- The legacy repository validator rejects a correct 0.1.36 `ready` Project whose Tasks remain `planned`; the 0.1.36 validator passes the current Project and removes premature current-Task routing.
- `skills/README.md`
- `skills/agent-organization/`
- `agents/context/projects/agent-organization-v2-migration/PROJECT.md`
- `agents/context/projects/agent-organization-v2-migration/PLAN.md`
- `agents/context/references/verification.md`

## Owned Scope

- Create: `agents/skills/` and `agents/context/projects/agent-organization-v2-migration/tasks/01_skill-family/REPORT.md`
- Modify: tracked content moved from `skills/` to `agents/skills/`, including `agents/skills/README.md` and the complete nested family
- Test: `agents/skills/agent-organization/agent-project-organization/scripts/test_validate_projects.py`, `agents/skills/agent-organization/agent-project-organization/scripts/validate_projects.py`, `agents/skills/agent-organization/agent-workflow-organization/scripts/validate_workflows.py`, recursive discovery, metadata matching, normalized source inventory, generated-file exclusion, Markdown links, and exact diff scope

## Do Not Touch

- Any file outside `skills/`, target `agents/skills/`, and this Task's `REPORT.md`
- Root `AGENTS.md`, `agents/context/references/verification.md`, access, MCP, tool, template, adapter, application, infrastructure, workflow-automation, archived Project, or legacy-run files
- `PROJECT.md`, `PLAN.md`, and Project routing

## Interfaces

- Consumes: the current complete 37-file repository family; the requested 0.1.36 non-generated 24-file subset; package ID, repository URL, version, and license; the current active Project records
- Produces: a complete 38-file target family under `agents/skills/`, updated portable provenance, passing target-path validators, recursive-discovery evidence, and `REPORT.md`

## Skills, Tools, Authority, And Selection

- Required implementation skills: `agent-skill-organization`, `testing-agent-skills`, and return through `agent-organization`
- Required review and verification skills: `requesting-code-review`, `receiving-code-review`, `testing-agent-skills`, and `verification-before-completion`
- Allowed tools and actions: read tracked repository files and the supplied 0.1.36 package source; make in-scope tracked moves and edits; run local deterministic checks; stage and commit exact owned paths
- Approval-gated actions: any deletion beyond removal of generated bytecode from the copied source, any change to retained metadata semantics, or any source/provenance choice beyond the resolved 0.1.36 package
- Prohibited actions: delegation; push, pull request, merge, deployment, publication, external mutation, secret or `.codex-local/` access, package installation, force push, destructive Git reset, or storing a machine-local path
- Implementer capability class: `deep` because the Task combines a structural move, semantic validator migration, provenance reconciliation, and compatibility preservation
- Implementer reasoning class: `high` because incorrect reconciliation could invalidate active Project state or discovery while appearing superficially successful
- Review depth: `rigorous` because the named compatibility risk is silent breakage of Project validation or recursive skill discovery across the structural and semantic migration
- Reviewer capability class: `deep` because review must independently compare source inventory, semantic deltas, metadata, and live Project behavior
- Reviewer reasoning class: `high` because the review must challenge compatibility assumptions and inspect verification coverage for the named risk
- Isolation requirements: one implementer writes only the skills surface and its report in this worktree; no other worker writes concurrently; Project control state remains coordinator-owned

## Implementation Contract

- One implementer session; do not delegate or subdivide.
- The brief contains sufficient task-local context; do not depend on the full coordinator conversation.
- Ask before guessing at a material ambiguity.
- Use the repository's equivalent evidence cycle: record the observed legacy-validator/path failure as red evidence, then prove the migrated paths and validator behavior green.
- Implement only this Task, run focused and required checks, inspect scope, and commit exact owned files when Git policy requires it.
- Write `REPORT.md`; do not update Project control state.
- If the Task cannot fit this contract, return `BLOCKED: OVERSIZED`.

## Verification And Evidence

- Focused check: compare normalized relative inventories and contents for the 24 supplied non-generated files, require exactly 14 retained metadata files, 14 unique folder-matched `SKILL.md` files, no generated bytecode, and no live `skills/` root
- Broader check: run `python -B agents/skills/agent-organization/agent-project-organization/scripts/test_validate_projects.py`, `python -B agents/skills/agent-organization/agent-project-organization/scripts/validate_projects.py .`, and `python -B agents/skills/agent-organization/agent-workflow-organization/scripts/validate_workflows.py .`; require `OK` and zero errors or warnings
- Diff or artifact review: inspect the exact task-base-to-head diff for only the old and target skills paths plus `REPORT.md`; distinguish moves, supplied semantic changes, retained metadata, and README provenance
- Required evidence: exact Task base and head commits, source/target inventory counts, validator test count and result, live validator results, recursive-discovery and metadata results, generated-file scan, local Markdown-link results, secret/cache-path scan, `git diff --check`, and scope status

## Ambiguity And Escalation

- The implementer may resolve mechanical line endings, Git rename detection, and README wording from the package metadata and current repository policy without broadening scope.
- Escalate before continuing if the source subset count differs from 24 non-generated files, retained metadata conflicts with a changed skill name or contract, the active Project fails the migrated validator, recursive discovery is not demonstrable, a secret or machine-local path could enter tracked output, or any required check cannot run.

## Reuse Assessment

Determine whether the work reveals a verified reusable procedure, recurring outcome, capability contract, runtime rule, access boundary, harness mapping, or stable context. Record the candidate and evidence in `REPORT.md`; do not silently expand this Task to promote it.

## Return And Review

- Implementer report: `REPORT.md`
- Expected return artifact: one exact local commit containing the skill-family move, 0.1.36 overlay, retained metadata, provenance update, and implementer report
- Independent review: `REVIEW.md`
- Review requirements: rigorously compare the exact source subset and target, trace validator and task-contract compatibility, verify recursive discovery and retained metadata, inspect provenance and machine-local-path exclusion, check actual Task diff and evidence, and return specification, quality, readiness, finding evidence, and re-review gate
- Allowed implementer statuses: `DONE`, `DONE_WITH_CONCERNS`, `NEEDS_CONTEXT`, `BLOCKED`, `BLOCKED: OVERSIZED`
