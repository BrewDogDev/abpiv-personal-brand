# Task 03: Integrate, Verify, And Prepare Publication

## Status

Ready

## Parent Project And Live Basis

- Project: `agents/context/projects/agent-documentation-mono-layout-migration/PROJECT.md`
- Plan row: 03
- Planned from: branch `codex/migrate-agent-docs-mono-layout` at coordinator head `5eec2ce`
- Refreshed at: 2026-07-27 after Task 01 and Task 02 amended independent verdicts returned `READY`
- Dependencies verified: Task 01 implementation `92fdb1a` plus revision `bff58ef`; Task 01 amended review `READY`; Task 02 implementation `ccb4225` plus revision `8db18c1`; Task 02 amended review `READY`; current worktree contains only the Task 02 review append awaiting this coordinator checkpoint

## Outcome And Acceptance

- Outcome: The integrated agent-documentation migration has complete canonical discovery, adapter, verification, and generated-cache hygiene; passes whole-Project validation and independent review; and supplies a clean, reconciled, publication-ready commit range for coordinator promotion through `preview` and merge to `main`.
- Acceptance criteria:
  - `agents/` contains only canonical context, while top-level `skills/`, `tools/`, `mcp-servers/`, `access/`, `adapters/`, and `templates/` are discoverable through active context and adapter documentation.
  - The repository map contains a direct top-level skill-family route; the Codex adapter links the canonical family and accurately describes its 14 source `agents/openai.yaml` metadata records without duplicating skill bodies or claiming a generated manifest.
  - The verification reference contains cache-safe canonical commands for 31 Project-validator tests, live Project validation, and Workflow validation.
  - `.gitignore` excludes Python `__pycache__`, `.pyc`, and `.pyo` outputs; a read-only ignore probe proves the rule without creating or reading secret-bearing local state.
  - No active path or command outside immutable Project archives, legacy runs, and this Project's historical evidence treats `agents/{skills,tools,mcp-servers,access,adapters,templates}/` as canonical.
  - The Mono 1.1.0 family remains exactly 37 non-generated files, with 35 exact source matches and only the three documented corrections in two files; 14 skills, 14 metadata mappings, 90 or more relevant rendered local links, 31 tests, both live validators, and 8/8 deterministic scenarios pass.
  - Changed and neighboring active Markdown links resolve; credential-free JSON examples parse; no private-key block, credential value, ignored local payload, installed-cache path, user-specific absolute path, or generated bytecode enters the tracked result.
  - The whole Project diff from `c58a221` changes only authorized agent documentation, `.gitignore`, and Project records; application code, implementation documentation, `.github/workflows/`, infrastructure, brand assets, immutable July 24 archive, and legacy run history are unchanged.
  - A fresh fetch and separate local-`main` inspection records every `main` and `preview` update since the Project base; overlapping agent-documentation changes stop the Task for coordinator replanning, while non-overlapping updates are reported for coordinator reconciliation before publication.
  - `REPORT.md` provides exact whole-Project commands, results, commit ranges, remote identities, review risks, reuse promotions, residual limitations, and a coordinator-ready publication/closure sequence without pushing, opening a PR, merging, deploying, or claiming future external state.

## Owned Scope

- Create: this Task's `REPORT.md`
- Modify: `.gitignore`; `agents/context/references/repository-map.md`; `agents/context/references/verification.md`; `adapters/codex/README.md`; root `AGENTS.md` or `README.md` only if a narrow inspected discovery gap requires it
- Test: the complete Project diff and all active agent-owner surfaces, source parity, recursive discovery, metadata, local links, JSON examples, validators, deterministic scenarios, ignore behavior, old paths, secrets/machine paths, immutable/prohibited scopes, Git graph, remote refs, and local `main`

## Do Not Touch

- `skills/` family or README, `access/`, `mcp-servers/`, `tools/`, `templates/`, historical Kilo adapter, implementation domains, `.github/workflows/`, immutable Project archives, legacy runs, prior Task records, ignored `.codex-local/`, external services, branch refs, remotes, pull requests, or deployments.
- `agents/context/projects/agent-documentation-mono-layout-migration/PROJECT.md`, `PLAN.md`, Project routing, handoff, archive placement, or any other coordinator control state.

## Interfaces

- Consumes: reviewed Task 01 and Task 02 outputs; Mono 1.1.0 source/package manifest as read-only evidence; active context and adapter contracts; verification and Git policy; current remote refs and clean local `main` observation
- Produces: integrated canonical discovery/verification/hygiene changes, whole-Project evidence, `REPORT.md`, one exact implementation commit, and a stable package for final independent review and coordinator publication/closure

## Skills, Tools, And Authority

- Required implementation skills: `mono:agent-organization`, `mono:agent-context-organization`, `mono:agent-adapter-organization`, `mono:agent-skill-organization`, `mono:self-improving-skill`, `mono:verification-before-completion`
- Required review and verification skills: `mono:requesting-code-review`, `mono:receiving-code-review`, `mono:verification-before-completion`
- Allowed tools and actions: read tracked files, Git metadata, selected Mono 1.1.0 source/manifest, and read-only GitHub/local-main state without persisting machine-local paths or credentials; fetch remote refs; use `apply_patch` for owned edits; run local parsers, tests, validators, comparisons, searches, and ignore probes; stage exact owned files; commit one coherent Task result
- Approval-gated actions: publication, push, pull-request creation, merge, archival, or handoff remain coordinator-owned after independent review even though the original request authorizes the eventual `main` merge
- Prohibited actions: delegate; edit Project control state; read ignored secrets; mutate external systems; push; open or merge a pull request; deploy; use destructive reset/clean/checkout or force push; change implementation behavior, workflows, immutable history, or unowned canonical bodies

## Implementation Contract

- One implementer session; do not delegate or subdivide.
- Ask before guessing at a material ambiguity.
- Use test-driven development or the repository's equivalent evidence cycle when behavior changes.
- Implement only this Task, run focused and required checks, inspect scope, and commit exact owned files when Git policy requires it.
- Write `REPORT.md`; do not update Project control state.
- If the Task cannot fit this contract, return `BLOCKED: OVERSIZED`.

## Verification And Evidence

- Focused check: prove the repository-map skill route, Codex skill/metadata mapping, cache-safe verification commands, and Python-cache ignore behavior, with an initial failing observation or approved documentation-only equivalent
- Broader check: run the complete 37-file normalized source comparison; 14 frontmatter and metadata mapping checks; `python -B skills/agent-organization/agent-project-organization/scripts/test_validate_projects.py`; live Project and Workflow validators; deterministic 8-scenario matrix; changed-and-neighboring active-link resolver; JSON parse; full Project old-path/safety/prohibited-scope checks
- Diff or artifact review: compare `c58a221..HEAD` and the Task dispatch base to Task head; require only Project-authorized documentation, `.gitignore`, Task records, and history-preserving moves; identify every remote/local-main commit since the recorded bases and stop on overlapping agent-documentation changes
- Required evidence: exact commands and counts; source/version/provenance; current `origin/main`, `origin/preview`, local `main`, topic base/head, and ahead/behind relations; no generated cache after `python -B`; `git diff --cached --check`; full staged diff and safety scan; exact commit; accepted behavioral limitation; promoted learning and rejected reuse candidates; coordinator publication and truthful two-phase archival sequence if needed

## Reuse Assessment

Use `self-improving-skill` to promote the verified top-level discovery, source-metadata, cache-safe validator, and generated-cache rules into the owned stable documents in this Task. Keep the one-use migration-verifier candidates in Project history because both independent reviews rejected promotion without recurring demand.

## Return

- Implementer report: `REPORT.md`
- Independent review: `REVIEW.md`
- Allowed implementer statuses: `DONE`, `DONE_WITH_CONCERNS`, `NEEDS_CONTEXT`, `BLOCKED`, `BLOCKED: OVERSIZED`
