# Task 08: Perform integrated migration verification

## Status

Ready

## Parent Project And Live Basis

- Project: `agents/context/projects/agent-organization-v2-migration/PROJECT.md`
- Plan row: 08
- Planned from: `codex/migrate-agent-organization-v2` at coordinator commit `3c7b8ad88b6f06b1930a7d68e3cefba530fa3991`
- Refreshed at: 2026-07-27 at coordinator commit `3c7b8ad88b6f06b1930a7d68e3cefba530fa3991`
- Project base: `origin/preview` commit `088ac31aeea018131a7bf4d11fff8943266cfba1`
- Dependencies verified: Tasks 01 through 07 are `complete`, each has `DONE` implementation evidence and `COMPLIANT` / `APPROVED` / `READY` independent review.

## Role, Outcome, And Acceptance

- Role: read-only whole-Project verification implementer
- Outcome: every migration completion criterion has fresh, integrated evidence and the complete local branch is ready for Project closure without publication.
- Acceptance criteria:
  - Verify the six former repository-root owner trees are absent and the canonical targets beneath `agents/` contain the reviewed inventory: 39 tracked Skills-owner files including its registry, 14 Access files, 2 MCP files, 1 Tool registry, 1 Template registry, and 3 adapter files.
  - Verify the `agent-organization` family contains the documented 24 non-generated version 0.1.36 supplied files plus 14 retained metadata files, 14 recursively discoverable unique folder-matched skills, 14 matching metadata prompts, no generated bytecode, and exactly the two documented portability corrections. Use repository-addressable reviewed provenance and tracked evidence; never persist or report an installed-cache path.
  - Verify root discovery, canonical context routes, repository-map rows, adapter mappings, Access-to-MCP relationships, validator commands, and every active local Markdown link relevant to agent infrastructure.
  - Verify active stale-path scans distinguish former repository-root owner routes from valid sibling-owner links, immutable history, this migration's own evidence, and unrelated generic implementation directories.
  - Verify root `AGENTS.md` and `README.md`, `agents/context/projects/archive/`, and `agents/context/runs/legacy/` are unchanged from Project base.
  - Run the 32-test Project-validator suite and the live Project and Workflow validators with Python bytecode generation disabled; require `OK` and zero errors or warnings.
  - Verify the Project-base-to-current diff contains only authorized tracked agent-infrastructure migration and Project-control evidence, with no application, infrastructure, workflow-automation, secret, runtime, permission, or external-state change.
  - Verify all prior Task reports and reviews exist, every completed Task has `COMPLIANT` / `APPROVED` / `READY` evidence, and no blocking concern remains.
  - Assess the recorded reuse candidates. Treat the reviewed source update policy in `agents/skills/README.md` and the canonical link/validator procedure in `agents/context/references/verification.md` as the durable integrations already produced; do not invent a Tool, Template, Workflow, or script for one-time migration checks without stronger recurring evidence.
  - Record exact Git state: branch, base, head, clean or dirty state, ahead/behind observation, and unpushed/unpublished status. Do not fetch, push, publish, open a pull request, merge, deploy, or mutate external state.

## Relevant Context And Source Paths

- `agents/skills/agent-organization/SKILL.md`
- `agents/skills/agent-organization/testing-agent-skills/SKILL.md`
- `agents/skills/agent-organization/agent-project-organization/SKILL.md`
- `agents/context/projects/agent-organization-v2-migration/PROJECT.md`
- `agents/context/projects/agent-organization-v2-migration/PLAN.md`
- Task 01 through Task 07 `TASK.md`, `REPORT.md`, and `REVIEW.md` artifacts
- `agents/skills/README.md`
- `agents/context/CONTEXT.md`
- `agents/context/ROUTING.md`
- `agents/context/references/repository-map.md`
- `agents/context/references/verification.md`
- `agents/adapters/`, `agents/access/`, and `agents/mcp-servers/`
- Root `AGENTS.md`, root `README.md`, immutable Project archives, and legacy runs

## Owned Scope

- Create: `agents/context/projects/agent-organization-v2-migration/tasks/08_integrated-verification/REPORT.md`
- Modify: no existing repository file
- Test: the complete migration branch and Project evidence using read-only local checks; stage and commit only `REPORT.md`

## Do Not Touch

- Any existing file, including Project control, active routing, migrated owners, canonical context, Task 01 through Task 07 artifacts, root entrypoints, applications, infrastructure, automation, archives, and legacy runs
- Installed consumers, plugin caches, `.codex-local/`, credentials, ignored local state, runtime, permissions, or external systems

## Interfaces

- Consumes: the complete reviewed migration diff, all Task return artifacts, Project completion criteria, selected version provenance, and current local Git state
- Produces: a fresh integrated verification report, explicit reuse disposition, exact Git state, and a rigorous whole-Project review package

## Skills, Tools, Authority, And Selection

- Required implementation skills: `agent-organization`, `testing-agent-skills`, and `verification-before-completion`
- Required review and verification skills: `requesting-code-review`, `receiving-code-review`, and `verification-before-completion`
- Allowed tools and actions: read tracked repository and local Git state; run deterministic validators, inventory, link, content, history, scope, and safety checks; create, stage, and commit only `REPORT.md`
- Approval-gated actions: any fix, new reusable artifact, source-version change, history rewrite, publication, external check requiring authentication, or runtime action
- Prohibited actions: delegation; editing an existing file; installed-cache or ignored-state inspection; secret access; external/runtime mutation; fetch, push, PR, merge, deploy, publish, permission change, force push, destructive reset, or cleanup of unrelated state
- Implementer capability class: `deep` because the Task integrates structural, semantic, provenance, history, safety, Project-control, and Git evidence
- Implementer reasoning class: `high` because the checks must distinguish live contracts, migration evidence, immutable history, reviewed deviations, and unrelated repository paths
- Review depth: `rigorous` because this is the final compatibility and preservation gate before Project closure
- Reviewer capability class: `deep` because the review must independently reproduce the whole-Project evidence from the Project base to the verification head
- Reviewer reasoning class: `high` because a plausible but incomplete scope, stale-path, provenance, or history check could allow silent migration failure
- Isolation requirements: one implementer writes only this Task report in the shared worktree; no other worker writes concurrently; the verifier must stop rather than repair any finding

## Implementation Contract

- One implementer session; do not delegate or subdivide.
- The brief contains sufficient Task-local context; do not depend on the full coordinator conversation.
- Ask before guessing at a material ambiguity.
- Use test-driven development or the repository's equivalent evidence cycle when behavior changes. This Task is verification-only, so record the Project-base negative condition or invariant and the current passing observation for each migration class.
- Make no fix. Any failed acceptance criterion or blocking finding returns `DONE_WITH_CONCERNS` or `BLOCKED` to the coordinator for replanning.
- Implement only this Task, run focused and required checks, inspect exact scope and Git state, write `REPORT.md`, and commit only that report when all required checks pass.
- Do not update Project control state.
- If the Task cannot fit this contract, return `BLOCKED: OVERSIZED`.

## Verification And Evidence

- Focused check: six former roots absent; target counts are 39/14/2/1/1/3; target anchor files and all cross-owner mappings resolve
- Skills check: family inventory 38; supplied/retained split 24/14; 14 unique folder-matched skills; 14 paired metadata prompts; two documented portability corrections; 15 local family links; no `__pycache__`, `.pyc`, or `.pyo`
- Active-links check: resolve local inline Markdown links across live agent infrastructure, with explicit exclusions and totals recorded; verify root/context/adapter/access/MCP mappings and no former-root live route
- Preservation check: compare Project-base blobs and diffs for root `AGENTS.md`, root `README.md`, `agents/context/projects/archive/`, and `agents/context/runs/legacy/`
- Project-evidence check: Tasks 01 through 07 each have a brief, `DONE` report, clean review verdicts, and exact implementation ranges; Task 08's brief exists and its report is the only permitted new write
- Broader check: run the 32-test Project-validator suite and live Project and Workflow validators with `-B`; require `OK`, zero errors or warnings, and no bytecode
- Diff or artifact review: classify every `088ac31aeea018131a7bf4d11fff8943266cfba1..HEAD` changed path; require only former owners, target `agents/*` infrastructure, and this Project's control/evidence; require `git diff --check`
- Safety check: no generated artifacts, private-key blocks, credential-shaped values, user-specific absolute paths, or concrete installed-cache paths in the migration diff
- Git check: exact branch/base/head, status, upstream comparison, commits ahead, and confirmation that no push or publication occurred
- Required evidence: exact Task base/head and commit, command or checker methods, counts and exclusions, failures or limitations, reuse disposition, residual risk, and rigorous review package

## Ambiguity And Escalation

- The implementer may choose deterministic read-only scripts or shell checks and may rely on reviewed repository-addressable provenance plus current tracked content. It may not record or inspect a machine-local installed source path.
- Escalate without editing if a target count differs, a prior verdict is missing, a live link or route fails, a validator warns, historical or root blobs changed, scope includes an unrelated domain, provenance cannot support the documented inventory, a secret-shaped value or local path appears, or Git publication state cannot be verified locally.

## Reuse Assessment

Compare the normalization-aware source overlay, owner migration, exact-blob registry, adapter relocation, and resolution-aware context check candidates recorded in Tasks 01 through 07. State in `REPORT.md` whether each reflects recurring repository work or one-time migration evidence; identify the durable integration already present; require separate reviewed planning before creating any new reusable artifact.

## Return And Review

- Implementer report: `REPORT.md`
- Expected return artifact: one exact local commit containing only the whole-Project verification report
- Independent review: `REVIEW.md`
- Review requirements: rigorously reproduce every completion criterion, owner and skills inventory, live link and stale-route result, history preservation claim, Project-evidence verdict, validator result, safety scan, whole-diff classification, reuse disposition, Git/publication state, report accuracy, and return specification, quality, readiness, findings, and re-review gate
- Allowed implementer statuses: `DONE`, `DONE_WITH_CONCERNS`, `NEEDS_CONTEXT`, `BLOCKED`, `BLOCKED: OVERSIZED`
