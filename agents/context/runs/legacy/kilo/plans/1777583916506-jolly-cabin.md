# Personal Brand Site - Review & Cleanup Plan

## Repository Overview

A Docusaurus v3 (TypeScript) personal brand site with four content sections:
- `/research` - Long-form research papers
- `/insights` - Blog-style posts
- `/newsroom` - Announcements
- `/featured` - Podcast appearances, interviews, talks

---

## Identified Issues / Template Stuff

### 1. GitHub Link Mismatch
**File:** `links.ts:2`
- Currently: `github: 'https://github.com/BrewDogDev'`
- Should this be `https://github.com/abpiv` (to match the org name in `docusaurus.config.ts`)?

### 2. Homepage Description TODO
**File:** `src/pages/index.tsx:9`
- Has: `description="TODO: Your tagline here"`
- Needs real tagline/content

### 3. Plausible Analytics Domain
**File:** `docusaurus.config.ts` (implied from HANDOVER.md)
- Analytics plugin expects a domain - is it configured?
- Should it be `abpiv.dev` or something else?

### 4. Author Files
- `research/authors.yml`, `insights/authors.yml`, `newsroom/authors.yml` - all contain the `allan` author definition with placeholder-style URL `https://abpiv.dev` (which may not exist)

### 5. Placeholder Image
**File:** `static/img/featured/placeholder.svg`
- Should be replaced with real featured item screenshots

### 6. Stub Docs
**File:** `docs/placeholder.md`
- Required to prevent search plugin crash during SSG (documented as a known limitation)
- Not linked anywhere (`routeBasePath: 'none'`)
- Can likely stay, but needs confirmation

### 7. Placeholder Content Seed Posts
The three seed posts (`research/2026-04-30-intro.mdx`, `insights/2026-04-30-welcome.mdx`, `newsroom/2026-04-30-site-launch.mdx`) are essentially template posts explaining the sections. They have generic content like example code blocks. Should they be replaced with real content?

### 8. About Page Removal
**File:** `src/pages/about.mdx`
- Remove the about page entirely - it's not linked in navbar so it's orphaned content
- The homepage hero already has the name, tagline, and brief info (CEO, affiliations) - that's sufficient
- Delete the about page file

### 9. Navbar About Link Removal
**File:** `docusaurus.config.ts:148-151`
- Remove the "About" item from the navbar items array since the page is being deleted

---

## Questions for You

1. **GitHub URL**: Should the GitHub link be `github.com/BrewDogDev` or `github.com/abpiv`? Or both?

2. **Site tagline**: What should go in the homepage description (currently `TODO: Your tagline here`)?

3. **Analytics domain**: What domain should Plausible analytics track? (`abpiv.dev`? A different domain? Skip analytics?)

4. **Author URL**: The `url` in `authors.yml` is `https://abpiv.dev` - does this domain exist/should it point somewhere?

5. **Seed content posts**: The three introductory posts are essentially "welcome to this section" templates. Should they be:
   - Deleted entirely?
   - Kept as-is for now?
   - Replaced with real first posts?

6. **Featured images**: The `featured/` folder has a `placeholder.svg`. Do you have real screenshots/thumbnails for featured items, or should we leave it for now?

7. **Stub docs**: The `docs/placeholder.md` is required for search to work. OK to leave as-is?

8. **Footer links**: The footer currently has a "Navigation" section with links to Research, Featured, Insights, Newsroom. Should this remain, or should it be replaced/removed?

9. **Any other cleanup**: Are there any other placeholder texts, broken links, or template artifacts you spotted that need addressing?