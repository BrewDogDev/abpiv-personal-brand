# Agent Workflow Registry

No reusable agent Workflow contract is currently registered.

Create a Workflow here only when a recurring outcome needs explicit stages, dependencies, gates, outputs, and run expectations. GitHub Actions files under `.github/workflows/` and n8n runtime workflows are implementation systems, not entries in this registry.

Workflow contract creation and changes belong to `agent-workflow-organization`.
