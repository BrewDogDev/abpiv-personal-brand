# Agent Template Registry

No reusable agent artifact template is currently registered.

Add a template only when repeated use demonstrates a stable structure that belongs to a canonical agent owner. Templates must not contain run-specific state, credentials, private payloads, or machine-local paths.
