# Agent Tool Registry

No repository-owned executable agent tool is currently registered.

An agent tool must expose one bounded executable capability with explicit inputs, outputs, safety rules, and verification. GitHub Actions workflows, n8n runtime workflows, scripts owned by an implementation domain, and prose procedures are not automatically agent tools.

Tool contract creation and changes belong to `agent-tool-organization`.
